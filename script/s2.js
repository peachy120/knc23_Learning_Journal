// JavaScript source code
// 6
console.log("Hello World!");

// 7a
// Define a variable named a and initialise it to the value of 5
let a = 5;

// 7b
// Log the value of a to the console. HINT: use console.log();
console.log("a : ", a);

// 7c
// Define a variable named b and initialise it to 3
let b = 3;

// 7d
// Log to the console the value of a multiplied by b
console.log("a * b = ", a * b);

// 7e
// Log to the console the value of a added to b, then modulo 3
console.log("a + b % 3 = ", (a + b)%3);

// 7f
// Define a variable s1 and initialise it to "Hello" and a variable s2 and initialise it to your name
let s1 = "Hello";
let s2 = "Kary";

// 7g
// Print to the console the result of concatenating s1 and s2
console.log(s1 + s2);

// 7h
// Improve the output of the previous step by adding a blank space between s1 and s2
console.log(s1 + " " + s2);

// 7i
// Log to the console the concatenated string a " squared is " a*a
console.log(a + " squared is " + (a * a));
